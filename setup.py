from distutils.core import setup

setup(
    name='image_processing',
    version='0.1dev',
    packages=[
      'improc',
      'improc.features',
      ],
    # long_description=open('README.txt').read(),
)
